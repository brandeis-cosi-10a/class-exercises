## Find the short words

Write a function `find_short_words(words, limit)` which takes a list of strings as a parameter, and returns a **new list**
containing only the words that have fewer than `limit` characters.
